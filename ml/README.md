# ML Pipeline

## Components
- preprocessing → data preparation
- features → feature extraction (audio, EEG)
- training → baseline retraining scripts (see [training/README.md](training/README.md))
- evaluation → where to find metrics / API reports (see [evaluation/README.md](evaluation/README.md))

## Flow
Raw Data → Preprocessing → Feature Extraction → Model → Evaluation
