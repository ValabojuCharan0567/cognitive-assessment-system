{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "root": "backend"
  },
  "deploy": {
    "root": "backend",
    "startCommand": "cd backend && gunicorn api.app:app --bind 0.0.0.0:$PORT"
  }
}
