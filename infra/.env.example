# Prefer the repo-root `.env.example` — copy it to `../.env` (repository root).
#
# Docker Compose loads `../.env` via `env_file` in docker-compose.yml.
# You can keep a second file here as `infra/.env` for compose-only overrides
# (optional). Do not commit real secrets.
